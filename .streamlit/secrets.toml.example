# Copy this file to secrets.toml and fill in your Oracle credentials
# For Streamlit Cloud, add these in the app settings under "Secrets"

[oracle]
user = "your_username"
password = "your_password"
dsn = "(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=your-host.oraclecloud.com))(connect_data=(service_name=your_service_name))(security=(ssl_server_dn_match=yes)))"
