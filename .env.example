# InstaCRM Environment Configuration
# Copy this file to .env and fill in the values

# Master Secret Key for token generation and encryption
# Generate a secure random key: python -c "import secrets; print(secrets.token_hex(32))"
IOL_MASTER_SECRET=your_master_secret_key_here

# IPC Authentication Key (used for local communication between components)
# Generate a secure random key: python -c "import secrets; print(secrets.token_hex(32))"
IOL_IPC_AUTH_KEY=your_ipc_auth_key_here

# Oracle Database Configuration (No wallet required - uses TLS connection string)
# Get your connection string from Oracle Cloud Console -> Database -> DB Connection -> TLS
DB_USER=your_database_username
DB_PASSWORD=your_database_password
DB_DSN=(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=your-db.oraclecloud.com))(connect_data=(service_name=your_service_name))(security=(ssl_server_dn_match=yes)))

# Oracle Database Credentials
# DO NOT COMMIT .env FILE TO GIT
DB_USER=your_db_username
DB_PASSWORD=your_db_password
DB_DSN=(description= (retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=xxxx)(host=adb.xx-xxxxxxx-1.oraclecloud.com))(connect_data=(service_name=xxxxxxxxxxxxxxxxx.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))
